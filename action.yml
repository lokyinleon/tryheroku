name: Docker Image CI

on:
  workflow_call:
    inputs:
      name:
        type: string
        required: true

jobs:

  build:
    steps:
    - uses: actions/checkout@v3
    - name: Send Log         
      run: |
        echo "Name: ${{ inputs.name }}"
